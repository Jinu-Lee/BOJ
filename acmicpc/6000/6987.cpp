#include <bits/stdc++.h>

using namespace std;

int arr[6][3];

int main() 
{
    ios::sync_with_stdio(0), cin.tie(0);

    for (int t = 0; t < 4; t++) {
        for (int i = 0; i < 6; i++) {
            for (int j = 0; j < 3; j++) {
                cin >> arr[i][j];
            }
        }

        
    }
}